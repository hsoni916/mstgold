import socket
import time
import sys

# host = 'api1.datakick.in'  # HOST
# port = 6660    # Port Number
# s = socket.socket() # Create Socket
# s.connect((host, port)) # Connect to socket
print("Socket Is Connected....")
# while True:
# 	msgrecv = s.recv(2048) # Recieve msg
# 	print(msgrecv)